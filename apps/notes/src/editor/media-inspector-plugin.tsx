/**
 * MediaInspectorPlugin — extracted to `@brainstorm/editor` (the shared
 * media stack). Re-exported here for the existing Notes import site
 * (`editor.tsx`).
 */

export { MediaInspectorPlugin } from "@brainstorm/editor";
